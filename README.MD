# ABAP Strict -- AI Code Generation Guide

## 🎯 Purpose

This repository defines the official ABAP coding standard for
AI-generated ABAP code.

All ABAP code generated by ChatGPT / OpenAI API must strictly follow
this guide.

Target Environment: - SAP S/4HANA 2022 - ABAP 7.56 - Clean ABAP
principles - Class-based development preferred

------------------------------------------------------------------------

# 🔥 AI Generation Rules (MANDATORY)

When generating ABAP code:

1.  Always generate COMPLETE executable code.
2.  Use ABAP 7.56 syntax (inline DATA, VALUE, NEW, etc.)
3.  Prefer class-based structure unless explicitly asked for REPORT.
4.  Avoid obsolete statements.
5.  Never use SELECT \*.
6.  Always handle exceptions explicitly.
7.  Provide meaningful variable names.
8.  Follow Clean ABAP naming style.
9.  Add minimal but meaningful comments.
10. No hard-coded credentials or API keys.

If requirements are unclear, make reasonable assumptions and clearly
list them before generating code.

------------------------------------------------------------------------

# 📐 Naming Convention

  Object           Rule
  ---------------- -----------
  Report           ZAI\_\*
  Class            ZCL\_\*
  Method           verb_noun
  Internal Table   lt\_\*
  Work Area        ls\_\*
  Variable         lv\_\*
  Constant         gc\_\*

------------------------------------------------------------------------

# 🧠 SELECT Rules

-   Explicit field list only
-   Use INTO TABLE @DATA(...)
-   Use ORDER BY when deterministic output needed
-   Use UP TO n ROWS when sample output required
-   Avoid nested SELECT inside LOOP

Example:

SELECT vbeln, erdat, kunnr FROM vbak INTO TABLE @DATA(lt_sales) WHERE
vkorg = @p_vkorg ORDER BY vbeln.

------------------------------------------------------------------------

# 📊 ALV Rules

-   Prefer CL_SALV_TABLE
-   No REUSE_ALV_GRID_DISPLAY
-   Handle exceptions
-   Use factory method

Example:

TRY. cl_salv_table=\>factory( IMPORTING r_salv_table = DATA(lo_alv)
CHANGING t_table = lt_sales ).

    lo_alv->display( ).

CATCH cx_salv_msg INTO DATA(lx_salv). MESSAGE lx_salv-\>get_text( ) TYPE
'E'. ENDTRY.

------------------------------------------------------------------------

# ⚙ Error Handling

-   Always use TRY...CATCH for risky operations
-   Raise meaningful messages
-   No silent failures

------------------------------------------------------------------------

# 🏗 Required Output Structure

AI-generated ABAP code must include:

-   TYPES
-   DATA
-   SELECTION-SCREEN (if needed)
-   START-OF-SELECTION
-   FORM or CLASS structure

------------------------------------------------------------------------

# 🚀 Prompt Usage Example

Generate ABAP code according to ABAP Strict README rules. Requirement:
Show 10 customers from company code 1000 in ALV.

------------------------------------------------------------------------

# 👨‍💻 Maintainer

Chimoo Lee\
SAP S/4HANA Logistics & ABAP Consultant
